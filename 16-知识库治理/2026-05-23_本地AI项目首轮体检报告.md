---
title: "本地AI项目首轮体检报告"
aliases:
  - "本地项目体检报告"
tags:
  - AI工具链
  - 本地项目
  - 代码维护
type: "project_audit"
status: "done"
quality_level: "A"
created: "2026-05-23"
updated: "2026-05-23"
summary_cn: "对 claude-to-im、llm-council、food-machinery-hero、persona-panel、telegram-watchdog 做首轮只读体检，记录运行入口和风险。"
related:
  - "[[AI工具链与本地项目维护总入口]]"
  - "[[业务总控台]]"
---

# 本地AI项目首轮体检报告

## 一句话结论

本地项目应按“先选一个项目 -> 读 README -> 看 git 状态 -> 跑测试/启动 -> 再改代码”的方式推进；当前最适合优先处理的是 `food-machinery-hero` 或 `claude-to-im`。

## 项目清单

| 项目 | 路径 | 类型 | 运行/验证入口 | 当前判断 |
|---|---|---|---|---|
| claude-to-im | `/Users/mac/projects/claude-to-im` | Node/TypeScript 库 | `npm run typecheck`、`npm run test`、`npm run build`、`npm run example` | 本地 TypeScript shim 修复后，`npm test` 通过，63 个单测全绿 |
| llm-council | `/Users/mac/projects/llm-council` | Python + 前端目录 | 后端当前 `main.py` 仅打印占位；前端在 `frontend/` | `uv run python -m py_compile main.py` 通过 |
| food-machinery-hero | `/Users/mac/projects/food-machinery-hero` | Next.js 前端 | `npm run dev`、`npm run build`、`npm run lint`、`npx playwright test` | 已修 lint；lint/build/Playwright 4 项测试通过；dev server 在 `localhost:3000` |
| persona-panel | `/Users/mac/projects/persona-panel` | Python 脚本 | README 指向 `persona-panel.py` 和环境变量 | `py_compile` 通过 |
| telegram-watchdog | `/Users/mac/projects/telegram-watchdog` | Python watchdog 脚本 | `telegram_watchdog.py`、`crewai_llm_stats.py` | `py_compile` 通过；未发现 README/package 配置 |

## Git 状态摘要

- `food-machinery-hero` 有未提交修改和未跟踪文件，包括 `package.json`、`src/app/*`、`src/components/`、`tests/`、`playwright.config.ts`、`project-screenshots/`、`test-results/`。
- 其他项目在本轮简查中没有输出明显 git 改动。
- 后续进入 `food-machinery-hero` 前，需要先读现有改动，不覆盖用户或前序任务成果。

## 下一步建议

1. 如果目标是业务展示和食品机械增长，先处理 `food-machinery-hero`。
2. 如果目标是 AI 工具链稳定，先处理 `claude-to-im`。
3. 如果目标是多模型顾问系统，先处理 `llm-council`。
4. `telegram-watchdog` 先做目录级体检，再判断是否是空项目、脚本项目或归档项目。

## 本轮修复记录

- `food-machinery-hero/src/components/SplineHero.tsx`：移除 effect 内同步重置 state，改为按加载完成的 scene URL 判断加载状态。
- `food-machinery-hero/src/components/ThreeDProductHero.tsx`：移除不必要的 mounted state，避免 React hooks lint 报错。
- `claude-to-im`：修复本地 `node_modules/.bin/tsc` / `tsserver` shim 指向，使 TypeScript 命令恢复可用。

## 本轮验证记录

- `claude-to-im`: `npm test` 通过，63 个测试通过。
- `food-machinery-hero`: `npm run lint` 通过，`npm run build` 通过，`npx playwright test` 通过，4 个测试通过。
- `llm-council`: `uv run python -m py_compile main.py` 通过。
- `persona-panel`: `python3 -m py_compile persona-panel.py src/crewai_panel.py src/minimax_llm.py` 通过。
- `telegram-watchdog`: `python3 -m py_compile telegram_watchdog.py crewai_llm_stats.py` 通过。
